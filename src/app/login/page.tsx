import { LoginForm } from '@/components/login-form'; // on va créer ça

export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <LoginForm />
    </div>
  );
}